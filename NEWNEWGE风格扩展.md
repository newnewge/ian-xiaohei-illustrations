---
type: skill-extension
doc: 小黑配图 · NEWNEWGE 风格扩展
parent: [[设计宪法v1.0-基准实例]]
status: active
version: 1.0
tags: [skill, 小黑, 设计宪法, 扩展, NEWNEWGE]
source: helloianneo/ian-xiaohei-illustrations
base_constitution: [[设计宪法v1.0-基准实例]]
---

# 小黑配图 · NEWNEWGE 风格扩展 v1.0

> 在保持小黑 IP 灵魂（怪诞 / 手绘 / 参与核心动作）的前提下，  
> 注入你的设计宪法（蓝色克制 / 视觉锤 / 高留白）。  
> 让 100 张配图既"有系列感"，又"像你画的"。

---

## 🎯 为什么需要这个扩展

| 上游默认 | 你的体系 | 冲突点 |
|----------|----------|--------|
| 纯白底 + 红橙蓝手写批注 | `#FAFAFA` 背景 + `#3498DB` 强调蓝 | 小黑原版的"红橙蓝"已经偏 warm，需要收口 |
| 自由手绘、不锁定视觉锤 | **强约束 4 件套视觉锤** | 上游没有视觉锤约定，需要你注入 |
| 黑色细线（默认） | `#2C3E50` 深蓝灰（克制理性） | 颜色从"纯黑"挪到"深蓝灰"才是你的味道 |
| 不锁定角色签名 | ✏️ tony 签名按场景 | 你的设计宪法硬约定 |
| 不强制留白比例 | 内容区 ≤ 60%，留白 ≥ 40% | 需要明确 |

> **扩展原则**：保留上游"怪诞清爽"的基因，**只替换**色盘/视觉锤/文字规则这 3 件最显性的东西。

---

## 🎨 你的色盘（NEWNEWGE 版）

### 主色盘

| 角色 | 原始版（上游） | NEWNEWGE 版 | 用途变化 |
|------|-----------|-------------|---------|
| 背景 | 纯白 `#FFFFFF` | `#FAFAFA` 极浅灰 | 与你的设计宪法对齐 |
| 主线 / 小黑轮廓 | 纯黑 `#000000` | `#1A1A1A` 深炭灰 | 不再"纯黑"，更克制 |
| 主强调批注 | 红色 `#E74C3C` | **保留** `#E74C3C`（≤5%） | 警示语义一致，沿用宪法 |
| 数据 / 信息 | 蓝色 `#3498DB` | **保留** `#3498DB` | 与宪法强调色一致 |
| 次强调批注 | 橙色（未锁定） | `#E67E22` 落日橙（≤3%） | 给上游的"橙色批注"一个 HEX 锚点 |

### 灰色阶梯（继承自宪法）

| 层级 | 色值 | 在小黑图里的用法 |
|------|------|------------------|
| 标题用 | `#1A1A1A` | 主标题文字 + 小黑描边 |
| 正文用 | `#4A4A4A` | 中文手写批注主色 |
| 注释用 | `#9A9A9A` | 辅助线 / 留白中的提示小字 |
| 分割线 | `#E0E0E0` | 工具台边缘 / 物件阴影 |

### 颜色冲突仲裁原则

```
当"小黑的怪诞感"与"宪法克制"冲突时：

1. 宪法级 Token（背景 / 主线 / 强调蓝 / 警示红）
   → 必须遵循宪法

2. 怪诞感依赖的颜色（落日橙 / 手绘批注的随机感）
   → 允许在 ≤3% 面积内自由发挥

3. 决策流程
   → 怪诞需要？ → 用宪法色板的"警示红 / 强调蓝 / 落日橙"
   → 克制需要？ → 用灰色阶梯
```

---

## 🔨 你的 4 件视觉锤（NEWNEWGE 版）

继承自 [[设计宪法v1.0-基准实例#组件层 3]]，在小黑图上重新定义：

### 视觉锤 1｜统一角标（必须出现）

```
┌────────────────────────────┐
│                            │
│       小黑怪诞正文          │
│                            │  ← 新NEWGE
│                ╔═══════╗   │
│                ║ 📍主理人 ║   │  ← 角标：右下，2px 主线边框，圆角 6px
│                ╚═══════╝   │
└────────────────────────────┘
```

| 项 | 规格 |
|----|------|
| 位置 | 右下角，距离右下边缘 80×80px |
| 边框 | 2px 主线 `#1A1A1A`，圆角 6px |
| 内容 | 📍 用途短句（如「📍认知锚点」「📍方法论」「📍金句」） |
| 字号 | Noto Sans SC Light 9pt |
| 配色 | 白底 + 主线边框 + 主线文字 |

> **作用**：一眼看出"这张图在你的公众号系列里"。

### 视觉锤 2｜统一分割线（标题下）

```
   主标题（≤15 字）
   ●─────────────────────────    ← 4px 主线圆点 + 0.5pt 灰色线
```

| 项 | 规格 |
|----|------|
| 位置 | 主标题正下方，左对齐 |
| 起点 | 4px 实心圆，颜色 `#1A1A1A` |
| 线宽 | 0.5pt 灰色 `#E0E0E0` |
| 长度 | 至主标题宽度对应位置（不贯穿整图） |
| 后留白 | 16px 后接主体内容 |

> **作用**：让"标题区"和"图主体"有清晰的分界，避开上游示例图容易出现的"无标题感"。

### 视觉锤 3｜序号系统（多张图成组时）

| 序号 | 视觉 | 含义 |
|------|------|------|
| 01 | 圆形，黑边 | 方法论 / 流程 |
| 02 | 方形，黑边 | 对比 / 转折 |
| 03 | 三角，黑边 | 视角 / 隐喻 |
| 04 | 菱形，黑边 | 案例 / 故事 |
| 05 | 圆形 + 蓝填充 | 数据 / 强调 |

| 项 | 规格 |
|----|------|
| 位置 | 图的左上角，角标同排 |
| 大小 | 24×24px |
| 描边 | 1.5pt `#1A1A1A`（蓝填充用 `#3498DB`） |
| 文字 | 内部无字，外部跟用途短句 |

> **作用**：多张图成组时（一篇文章 4-8 张），一眼能看清结构顺序。

### 视觉锤 4｜统一数据标注（如有数据）

按 `[[设计宪法v1.0-基准实例#视觉锤 4]]` 原文继承：

- 大数字：Inter Bold，72pt
- 数据来源：右下角 9pt 灰色
- 关键指标：4px 主线圆点 + 0.5pt 灰色线（与视觉锤 2 同款）

> 在小黑图里**条件触发**——只有当图内有数字时启用。

### 视觉锤 5（NEWNEWGE 新增）｜签名按场景

继承自 `[[DEC-20260718-003-视觉锤与tony签名三场景规则]]`：

| 场景 | 角标内的尾签 | 例子 |
|------|--------------|------|
| 🎓 培训 / 教学 | `✏️ tony` | 「📍认知锚点 ✏️ tony」 |
| 📦 正式交付 / 公开文章 | **不加** | 「📍认知锚点」 |
| ⚠️ 受限 / 草稿 / 风险 | 加灰字免责声明 | 「📍认知锚点 · 草稿·未校核」 |

---

## 📐 画布与空间规则

| 项 | 上游默认 | NEWNEWGE 版（与宪法一致） |
|----|---------|--------------------------|
| 比例 | 16:9 | 16:9（不变） |
| 画布尺寸 | 任意 | 1600 × 900 px |
| 留白率 | 40-60% | 主体 ≤ 60%，留白 ≥ 40% |
| 小黑尺寸 | 自由 | 占画面高度 25-35%（保证动作清晰） |

---

## ✍️ 中文批注规则（与宪法对齐）

| 项 | 规则 |
|----|------|
| 字数 | ≤ 6 字 / 标注，超过即拆 |
| 字体 | Noto Sans SC（图中渲染时使用图模型内置手写感） |
| 颜色 | 主色 `#1A1A1A` + 强调 `#3498DB` ≤ 2 处 + 警示 `#E74C3C` ≤ 1 处 |
| 位置 | 不要挤在四角，沿着小黑动作轨迹自然落下 |
| 错字 | 错字超过 1 个 → 重生成 |

---

## 🆎 与"上游风格"的差异速查

| 维度 | 上游 Ian 原版 | NEWNEWGE 版 |
|------|-------------|-------------|
| 背景 | 纯白 | `#FAFAFA` 极浅灰 |
| 主线 | 纯黑 | `#1A1A1A` 深炭灰 |
| 强调色 | 红 / 橙 / 蓝三色无锁定 | 红 `#E74C3C` ≤5% / 蓝 `#3498DB` / 落日橙 `#E67E22` ≤3% |
| 视觉锤 | 无强制 | **强制 4 件套**（角标 / 分割线 / 序号 / 数据标） |
| 签名 | 无 | 按场景三态（培训 / 交付 / 受限） |
| 角色 | 小黑一个人 | **小黑 + 可选"tony 影子"**（见下） |

### 关于"tony 影子"（可选增强）

如果你想让图更有你个人风格，可以在图右下角加一个**极小的 tony 剪影**：

```
┌────────────────────────────┐
│                            │
│  [小黑做核心动作]            │
│                            │
│                  ╔═════╗   │
│                  ║ 📍...║   │ ← 角标
│                  ╚═════╝   │
│                       ✏️   │ ← tony 影子，仅占 30×30px，灰色
└────────────────────────────┘
```

> **触发条件**：🎓 培训 / 教学场景启用；📦 正式交付禁用；⚠️ 受限场景用灰字 ⚠️ 替代。

---

## 📋 4 档调用模板（升级版，覆盖 README 里的原模板）

### 模板 1｜出 shot list，NEWNEWGE 风

```text
Use $ian-xiaohei-illustrations（NEWNEWGE 风）。
请分析下面这篇文章哪里值得配图，输出 5 张左右的 shot list。
每张图写清楚：
- 放在哪段后
- 主题
- 核心意思
- 结构类型
- 小黑在做什么
- 4 件视觉锤的安排（角标短句 / 序号 / 是否有数据标）
- 建议中文标注词（≤ 6 字）

调色盘：背景 #FAFAFA / 主线 #1A1A1A / 强调蓝 #3498DB / 警示红 #E74C3C（≤5%）/ 落日橙 #E67E22（≤3%）
签名：📦 正式交付不加 ✏️ tony。

【粘贴你的公众号正文】
```

### 模板 2｜直接出图，NEWNEWGE 风

```text
Use $ian-xiaohei-illustrations（NEWNEWGE 风）把下面这篇文章生成 4 张正文配图。

视觉硬约束：
- 16:9 横版 / 1600×900 px
- 背景 #FAFAFA（不是纯白）
- 主线 #1A1A1A（不是纯黑）
- 强调色只用 #3498DB（蓝）和 #E74C3C（红，≤5%）；橙色 #E67E22 ≤3%
- 黑色手绘线稿（细线、轻抖）
- 大量留白（主体 ≤ 60%）
- 少量中文手写批注（≤ 6 字）
- 4 件视觉锤必装：右下角标 + 标题下分割线 + 序号 + 数据标（如有数据）

签名：📦 正式交付不加 ✏️ tony。
文字：Noto Sans SC。
输出到：00-项目/<项目名>/illustrations/

【粘贴文章】
```

### 模板 3｜为一句话补一张图，NEWNEWGE 风

```text
Use $ian-xiaohei-illustrations（NEWNEWGE 风）为下面这句话生成一张正文配图：

"<你的金句>"

风格硬约束：
- 16:9 / 背景 #FAFAFA / 主线 #1A1A1A
- 必须 4 件视觉锤，右下角标写「📍金句」
- 小黑承担核心动作，绝不站着
- 强调红 #E74C3C 单图 ≤ 1 处
- 中文标注 ≤ 6 字
```

### 模板 4｜改图 / 去错字，NEWNEWGE 风

```text
Use $ian-xiaohei-illustrations 帮我编辑这张图：
- 去掉左上角的"<某标题>"
- 4 件视觉锤保留原位
- 调色盘保持 NEWNEWGE 版（#FAFAFA + #1A1A1A + #3498DB + #E74C3C）
- 重新生成 16:9 PNG
```

---

## ✅ QA checklist（NEWNEWGE 版，在上游清单基础上叠加）

上游 7 条 + NEWNEWGE 增量 6 条 = **共 13 条**：

### 继承自上游（`references/qa-checklist.md`）

- [ ] 16:9 横版
- [ ] 背景干净（现在锁定为 `#FAFAFA`）
- [ ] 小黑参与核心动作，不是装饰
- [ ] 中文标注 ≤ 6 字、错字 ≤ 1 个
- [ ] 没复刻上游 8 张示例图的构图
- [ ] 不是 PPT 风 / 不是可爱卡通
- [ ] 主体 ≤ 60%，留白 ≥ 40%

### NEWNEWGE 增量（必检）

- [ ] **视觉锤 1**：右下角标出现，2px 主线边框
- [ ] **视觉锤 2**：主标题下"4px 圆点 + 灰色线"出现
- [ ] **视觉锤 3**：左上序号标识与该图在文章中的位置一致
- [ ] **视觉锤 4**：若有数据，Inter Bold 数字 + 来源标注
- [ ] **签名场景**：📦 正式交付无 tony 签名；🎓 教学有 ✏️ tony；⚠️ 受限有灰字免责声明
- [ ] **色盘限制**：红色面积 ≤ 5%，橙色 ≤ 3%，不出现宪法外的颜色

---

## 🔄 升级路径

| 阶段 | 触发 | 动作 |
|------|------|------|
| v1.0（当前） | 首次 fork | 锁定色盘 / 视觉锤 / 签名规则 |
| v1.1 | 累计 10 张图后 | 收集反馈清单，决定是否微调强调色比例 |
| v2.0 | 设计宪法升级时 | 整盘重对齐 [[设计宪法v1.0-基准实例]] |

---

## 🗂 关联

- 父本 skill：`README_INTEGRATION.md`
- 品牌依据：[[设计宪法v1.0-基准实例]]
- 设计师：[[单人总控卡]]
- Fork 说明：`NEWNEWGE.md`
- 上游原作：[helloianneo/ian-xiaohei-illustrations](https://github.com/helloianneo/ian-xiaohei-illustrations)

---

## 🗂 变更记录

| 版本 | 时间 | 修改 |
|------|------|------|
| v1.0 | 2024-08 | 首版：色盘对齐宪法 / 4 件视觉锤 / 签名三态 / 4 档调用模板 / 13 条 QA |
